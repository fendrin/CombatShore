[unit_type]

    name= _ "Big Aircraft Carrier"    
    id=361
    [base_unit]
        id=vehicle
    [/base_unit]

    description= _ "Aircraft Carriers are very expensive floating airbases. It allows for an aerial campaign into enemy territory, by eliminating the need for aircraft to return to base. A Carrier may refuel, repair and rearm any aircraft that can land on it. Due to their size, some large airplanes can not land on Carriers. The Carrier itself has some weapons, but they are not suited for heavy combat."

    race=heavy_ship
    {HEAVY_SHIP}

    hitpoints=1900
    vision=70
    jamming = 0
    weight=4000
    # Tank
    # Energy = 0
    # Material = 70000
    fuel_tank=150000
    fuel_consumption=40

    [abilities] # Features = MoveDuringReactionFire Repair_Facility ExternalMaterialTransfer self_repairing sonar
        {FEATURE_SELF_REPAIR 8} # SelfRepairRate = 8
        {FEATURE_SONAR}
    [/abilities]

    # WreckageObject = 481
    # MaxSurvivableStorm = 255
    # ResourceDrillingRange = 0
    # WaitForAttack = false

    {TRANS_HVY_CARRIER} # Transportation ->* Vehicletype.TransHvyCarrier 

    # Height = floating 
    {FLOATING 51} # Movement = 0 0 51 0 0 0 0 0
    variation=floating
 
    {VERY_BIGSHIP_TERRAIN} # TerrainAccess ->* VehicleType.very_bigship_terrain

    # MovementSound = med_ship_a_s.ogg

[/unit_type]
